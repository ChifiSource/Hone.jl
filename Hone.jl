using Compose
tree(b) = introspect(b.img)
function Donut(data)
    predict(xt) = (xt = [i = a + (b * i) for i in xt])
    show() = img
    (test)->(variable;variable;predict)
end
function Pie(data)
    predict(xt) = (xt = [i = a + (b * i) for i in xt])
    show() = img
    (test)->(variable;variable;predict)
end
